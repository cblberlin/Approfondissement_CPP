#pragma once

#include <utility>
#include <map>

using namespace std;

class Arete{
    private:
        pair<int, int> arete;
    public:
        // constructeur
        Arete(int, int);

        // destructeur
        
};