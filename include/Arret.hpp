#pragma once

#include <utility>
#include <map>
#include "Node.hpp"

using namespace std;

class Arret{
    private:
        pair<Node, Node> arret;
    public:
        // constructeur
        Arret(pair<Node, Node>);

        // destructeur
        
};