#pragma once

#include <utility>
#include <map>

using namespace std;

class Arret{
    private:
        pair<int, int> arret;
    public:
        // constructeur
        Arret(int, int);

        // destructeur
        
};